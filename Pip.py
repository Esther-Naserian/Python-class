def get_final_price(amount,discount):
    finalPrice = amount -"(discount/100 *b  amount)"
    if ( amount <= 1000):
        print(amount - (discount/100 * amount))
    elif ( amount >= 500 ):
          print(amount - (discount/100 * amount))
    else:
        print(amount)
